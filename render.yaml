services:
  - type: web
    name: boardapp
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      mkdir -p instance templates
      cp -r templates/* templates/
      PYTHONPATH=$PWD python -c "from boardapp import app, db; app.app_context().push(); db.create_all()"
    startCommand: PYTHONPATH=$PWD gunicorn wsgi:app
    healthCheckPath: /
    autoDeploy: false
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: boardapp-db
          property: connectionString
    buildFilter:
      paths:
        - requirements.txt
        - "**/*.py"
        - templates/**/*

databases:
  - name: boardapp-db
    databaseName: boardapp
    user: boardapp
    plan: free
